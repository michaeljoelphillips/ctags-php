parameters:
    level: 6
    paths:
        - src
        - tests
    excludes_analyse:
        - tests/fixtures
    ignoreErrors:
        - '#Call to function iterator_to_array\(\) on a separate line has no effect.#'
